# Auto-launch pi inside tmux if not already running in one

pi() {
    if [ -n "$TMUX" ]; then
        command pi "$@"
    else
        tmux new-session -A -s pi "pi $*; read -p 'Done. Press Enter...'"
    fi
}
