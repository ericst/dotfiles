# Clearing bullshit set by others...
PROMPT_COMMAND=""

# Source global definitions first
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi


# Most of this is taken from the article titled [[https://sanctum.geek.nz/arabesque/better-bash-history/][Better Bash History]].

# First we want to append to the history instead of rewriting it. We also want to
# keep as much as possible into our history.
shopt -s histappend
HISTFILESIZE=1000000
HISTSIZE=1000000


# Of course, not everything is worth saving. For instance duplicate or empty lines
# are not interesting. Setting ~HISTCONTROL~ to the correct value will allow us to
# not save them. Also, some commands, such as the ones used for job control or
# simply listing contents of a directory, are not conveying any particular meaning
HISTCONTROL=ignoreboth
export HISTIGNORE="exit:ls:bg:fg:jobs:history"

# Recording not only the command but also the date at which it was recorded could
# prove useful. Setting ~HISTTIMEFORMAT~ will do just that.
# We also want to make sure that we save multilines together. Those are the shopt
shopt -s cmdhist
shopt -s lithist
HISTTIMEFORMAT='%F %T '

# This is to make sure I don't loose history. It forces bash to write each
# command to the history file immediately.
PROMPT_COMMAND="history -a; $PROMPT_COMMAND"


# Now we only need to make a small alias to search easily into the history.
alias hs="history | grep"


# When using a shell directory navigation is important to put yourself in the
# right context.
# We want bash to help us as much as possible. ~autocd~ allows us to change
# directory by only typing its name without even issuing the ~cd~
# command. ~cdspell~ and ~dirspell~ will auto correct types.
shopt -s autocd
shopt -s dirspell
shopt -s cdspell
CDPATH=":${HOME}:${HOME}/Projects/"

# Here are some safety nets. 

# ~rm~ will refuse to delete ~/~ and prompt confirmation whenever I try to delete
# more than 3 files.
# ~mv~, ~cp~ and ~ln~ will always ask before overwriting anything.
# ~ch{own,mod,grp}~ won't allow me to change permissions on ~/~
alias rm='rm -I --preserve-root'

alias mv='mv -i'
alias cp='cp -i'
alias ln='ln -i'

alias chown='chown --preserve-root'
alias chmod='chmod --preserve-root'
alias chgrp='chgrp --preserve-root'


# My editor of choice is Neovim. Below I set environment variables to make sure
# that it is correctly selected by other applications. I also set an alias to
# edit files quicker.
export ALTERNATE_EDITOR="vi"
export EDITOR="nvim"
export VISUAL="nvim"
export GIT_EDITOR="$EDITOR"
alias e="$VISUAL"

# Also, if I type vim, i meant nvim... If it is installed of course.
if type nvim &> /dev/null; then
    alias vim='nvim'
    export MANPAGER='nvim +Man!' # links man....
fi


# It is nice to have colors automatically in grep.
alias grep='grep --color=auto'


# aliases for listing processes and looking for specific ones
alias ps="ps auxf"
alias psl="ps auxf | less"
alias psg="ps aux | grep -v grep | \grep -i -e VSZ -e"


# Per default I want to create any missing directory.
alias mkdir="mkdir -pv"


# The function ~mkcd~ is extremely useful, to create a directory and immediately
# change to it.
mcd () {
    mkdir -p "$1"
    cd "$1" 
}

# Run the check when bash starts (but not for non-interactive shells)
if [[ $- == *i* ]]; then
    timeout 5 $HOME/.local/bin/dotfiles-git-check
fi

#Distrobox integration
workbox() {
    local fedora_version=$(grep -oP 'VERSION_ID=\K\d+' /etc/os-release)
    local image="fedora-toolbox:${fedora_version}"
    
    # Check if container exists without trying to enter it
    if distrobox list | grep -q "workbox"; then
        distrobox enter workbox
    else
        echo "Creating workbox from ${image}..."
        if distrobox create --pull --name workbox --image "${image}"; then
            echo "Container created successfully. Entering workbox..."
            distrobox enter workbox
        else
            echo "Failed to create container"
            return 1
        fi
    fi
}

# Docker is podman
alias docker='podman'

# Nice aliases
alias wb='workbox'
alias o='xdg-open'

cdtmp(){
    pushd $(mktemp -d)
}

#PROMPT
_git_prompt=/usr/share/git-core/contrib/completion/git-prompt.sh
[ -f "$_git_prompt" ] && source "$_git_prompt"
GIT_PS1_SHOWDIRTYSTATE=1
GIT_PS1_SHOWUNTRACKEDFILES=1

_build_ps1() {
    local exit_code=$?  # Must be first
    local G='\[\e[1;32m\]' R='\[\e[0m\]' E='\[\e[1;31m\]'

    # Container (purple, only when set)
    local container=""
    [ -n "$CONTAINER_ID" ] && container="[${CONTAINER_ID}] "

    # Jobs (only shown when there is at least one)
    local running stopped jobs_str=""
    running=$(jobs -r -p | wc -l)
    stopped=$(jobs -s -p | wc -l)
    if [ "$running" -gt 0 ] || [ "$stopped" -gt 0 ]; then
        local inner=""
        [ "$running" -gt 0 ] && inner+="${running}r"
        [ "$stopped" -gt 0 ] && inner+="${stopped}s"
        jobs_str="{${inner}} "
    fi

    # Git
    local git=""
    declare -f __git_ps1 &>/dev/null && {
        local b; b=$(__git_ps1 "%s")
        [ -n "$b" ] && git=" (${b})"
    }

    # Exit code (only when non-zero)
    local exit_str=""
    [ "$exit_code" -ne 0 ] && exit_str="${E}${exit_code}${R} "

    PS1="${G}${jobs_str}${container}\u@\h \w${git} ${R}\n${exit_str}\$ "
}

PROMPT_COMMAND="_build_ps1${PROMPT_COMMAND:+; ${PROMPT_COMMAND}}"


# Rebuild PATH with correct ordering after system initialization.
# Ensures /usr/local/* comes before /usr/*, removes all duplicates (including
# trailing-slash variants), and keeps any extra dirs (e.g. shelltape/bin).
#
# Root cause of old bug: `for dir in $PATH` with default IFS never splits on
# colons, so $PATH was treated as one big word and re-appended wholesale.
# Fix: set IFS=: when iterating, and use a three-pass strategy.
rebuild_path() {
    local new_path="" seen="" dir

    # Append dir to new_path only if it exists on disk and hasn't been seen yet.
    # Strips trailing slashes before comparing so /foo/ and /foo are treated equal.
    _path_add() {
        local d="${1%/}"                              # normalize trailing slash
        [ -d "$d" ] || return                         # skip non-existent dirs
        [[ ":$seen:" == *":$d:"* ]] && return         # skip duplicates
        new_path="${new_path:+$new_path:}$d"
        seen="${seen:+$seen:}$d"
    }

    # Pass 1 — user home dirs first, preserving their original relative order
    local IFS=:
    for dir in $PATH; do
        dir="${dir%/}"
        [[ "$dir" == "$HOME"* ]] && _path_add "$dir"
    done

    # Pass 2 — known system dirs in explicit priority order
    _path_add /usr/local/sbin
    _path_add /usr/local/bin
    _path_add /usr/sbin
    _path_add /usr/bin
    _path_add /usr/lib64/ccache
    _path_add /sbin
    _path_add /bin

    # Pass 3 — anything else not yet captured (custom tools, future additions)
    for dir in $PATH; do
        _path_add "$dir"
    done

    PATH="$new_path"
    unset -f _path_add
}
# Ensure ~/.local/bin is in PATH if it exists
if [ -d "$HOME/.local/bin" ] && [[ ":$PATH:" != *":$HOME/.local/bin:"* ]]; then
    PATH="$HOME/.local/bin:$PATH"
fi

rebuild_path
export PATH


# Source everything into a ~/.bashrc.d.
# TODO move configuration there, to make it nicer
if [ -d ~/.bashrc.d ]; then
    for rc in ~/.bashrc.d/*; do
        if [ -f "$rc" ]; then
            . "$rc"
        fi
    done
fi
unset rc

# Load Ghostty integration last so its hook runs after our prompt is built.
# Use installed resources, including the host installation inside Distrobox.
# Do not reuse the old cache: package timestamps cannot reliably refresh it.
if [[ $- == *i* && ${TERM_PROGRAM:-} == ghostty ]]; then
    for _ghostty_resources in \
        "${GHOSTTY_RESOURCES_DIR:-}" \
        /usr/local/share/ghostty \
        /usr/share/ghostty \
        /run/host/usr/local/share/ghostty \
        /run/host/usr/share/ghostty; do
        _ghostty_resources=${_ghostty_resources%/}
        [[ -n $_ghostty_resources ]] || continue
        [[ $_ghostty_resources == "$HOME/.cache/ghostty/resources-copy" ]] && continue
        if [[ -r $_ghostty_resources/shell-integration/bash/ghostty.bash ]]; then
            export GHOSTTY_RESOURCES_DIR="$_ghostty_resources"
            builtin source "$_ghostty_resources/shell-integration/bash/ghostty.bash"
            break
        fi
    done
    unset _ghostty_resources
fi


# References:
# https://sanctum.geek.nz/arabesque/better-bash-history/ - Better Bash History
# https://www.gnu.org/software/bash/manual/html_node/index.html - Bash Reference Manual
# https://tiswww.case.edu/php/chet/readline/readline.html - GNU Readline Library
